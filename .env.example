# Temporary API key for this assessment only.
# This key is intended to be short-lived and revocable after the session.
# Replace the placeholder below with the key provided by the interviewer,
# and ensure your solution loads it from the environment (e.g. via python-dotenv).
# Do not commit real keys to version control.

OPENAI_API_KEY=sk-your-temporary-key-here
